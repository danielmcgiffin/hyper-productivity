CI=1 NG_FORCE_AUTOCOMPLETE=false npm run lint

if [ "${RUN_PUSH_TESTS:-0}" = "1" ]; then
  CI=1 NG_FORCE_AUTOCOMPLETE=false npm run test
else
  echo "Skipping pre-push tests. Set RUN_PUSH_TESTS=1 to enable them."
fi
